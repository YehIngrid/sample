# 服務條款與隱私權政策（policy）

`src/policy/policy.html` / `policy.css`（無 `.js`）

## 用途
純靜態法律文件頁，用 `#privacy` / `#terms` anchor 錨點跳轉對應章節（`account.html` 的條款 Modal、footer 連結都會連到這裡的對應 hash）。

## 備註
沒有 JS 互動邏輯，改版時只需要動 HTML/CSS。
